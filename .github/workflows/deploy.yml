name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './public'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
---

## 🔍 核心变化说明

| 变化项 | 原配置 | 新配置 | 原因 |
|--------|--------|--------|------|
| **构建步骤** | ✅ Node.js + npm install + build | ❌ 全部删除 | 静态HTML不需要构建 |
| **上传路径** | `./dist` | `./public` | 部署源目录改变 |
| **构建时间** | 30-90秒 | <10秒 | 跳过依赖安装 |

---

## ✅ 提交步骤

1. **粘贴配置**后，滚动到页面底部
2. **Commit message填写**：`fix: 简化workflow，直接部署public静态文件`
3. **点击绿色按钮** `Commit changes`

---

## 🚀 提交后自动发生的事情
```
触发Actions → 拉取代码 → 打包public目录 → 部署到Pages
         ↓
      约5-15秒完成（无需npm构建）
         ↓
    等待你启用GitHub Pages设置
